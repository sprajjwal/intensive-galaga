<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GALAGA</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link rel="stylesheet" href="/static/css/main.css">
    {{!-- this is for adding more css in --}}
    {{{_sections.head}}}
</head>
<body>

  {{!-- NAVBAR --}}
  <nav class="navbar navbar-expand-lg navbar-dark gal-nav">
    <a class="gal-brand mx-auto" href="/">Space Shooter</a>
    <a class="nav-link mr-5 gal-text" href="/multiplayer">Multiplayer</a>
  </nav>  

  <div class="gal-body">
    <div id="gal-container">
      
      {{!-- PUT STUFF FOR BODY IN HERE --}}
      {{{body}}}
    </div>
    <div class="gal-highscore">
      <table class="table table-hover table-dark mb-0">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Score</th>
          </tr>
        </thead>
        <tbody>
          {{#each highscores}}
            <tr>
              <td>{{this.rank}}</td>
              <td>{{this.name}}</td>
              <td>{{this.score}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>
      
    </div>
  </div>
  
    

  {{!-- add scripts in here --}}
  {{{_sections.script}}}
</body>
</html>